$: << 'lib/cantiere/lib'
$: << 'lib'

require 'cantiere/helpers/rake-helper'
 
Cantiere::RakeHelper.new

require 'rake_support'

load 'common.rake'
load 'dist.rake'
load 'platform.rake'
#load 'docs.rake'

